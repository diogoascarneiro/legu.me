      <div class="card m-3" style="width:20%">
 <a href="#"><img class="card-img-top" src="{{this.images.REGULAR.url}}" alt="{{this.label}}"></a> 
  <div class="card-body p-1">
    <a href="#"><h5 class="card-title">{{this.label}}</h5></a> 
    <p class="card-text">Calories: {{this.calories}}</p>
    <p>Source:</p>
    <a href="{{this.url}}" class="btn btn-primary">{{this.source}}</a>
    <a href="#makefavourite">♡</a>
  </div>
</div>

